{
  "name": "arena-sanity-sync",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "pnpm -r -F \"./packages/*\" run build",
    "dev:nuxt": "pnpm -C examples/nuxt-app dev",
    "build:nuxt": "pnpm -C examples/nuxt-app build",
    "preview:nuxt": "pnpm -C examples/nuxt-app preview",
    "install:nuxt": "pnpm -C examples/nuxt-app install"
  },
  "devDependencies": {
    "@types/node": "^24.6.2"
  }
}
