{
  "name": "arena-sanity-sync",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "pnpm -r -F \"./packages/*\" run build",
    "build:core": "pnpm -C packages/core build",
    "publish:core": "pnpm -C packages/core publish --access public"
  },
  "devDependencies": {
    "@types/node": "^24.6.2",
    "typescript": "^5.9.3"
  }
}
