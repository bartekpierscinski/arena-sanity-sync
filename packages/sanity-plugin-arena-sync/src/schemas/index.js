export {arenaBlockSchema} from './arenaBlock'
export {arenaChannelSettingsSchema} from './arenaChannelSettings'
export {arenaSyncConfigSchema} from './arenaSyncConfig'

/** Schemas auto-registered by the plugin (areNaBlock + arenaSyncConfig) */
export const arenaSchemas = [
  // Lazy imports resolved at plugin init
]
